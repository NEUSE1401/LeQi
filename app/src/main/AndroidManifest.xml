<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="la.neu.leqi">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PersonalInformationActivity"
            android:theme="@style/CustomTitleTheme" />
        <activity
            android:name=".ChangePersonalInfoActivity"
            android:theme="@style/CustomTitleTheme" />
        <activity
            android:name=".ShopActivity"
            android:theme="@style/CustomTitleTheme" />

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".BicycleShopListActivity" />
        <activity
            android:name=".ConcreteGoodActivity"
            android:theme="@style/CustomTitleTheme"></activity>
        <activity android:name=".ActivityListActivity" />
        <activity android:name=".LogAndRegesterActivity"></activity>
    </application>

</manifest>